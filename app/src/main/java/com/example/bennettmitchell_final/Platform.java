package com.example.bennettmitchell_final;

// awwawawwaawrwaawarwarwwawr
public class Platform extends Status {
    // the classes are technically the same but this makes it easier to branch out. ie get games of platform.
    // essentially i just dont want to write the code twice
    public Platform(String name) {
        super(name);
    }
    public Platform(String name, int id, byte[] icon){
        super(name, id, icon);
    }

    // delete this but not yet because i want to show that its in the commit >:3c
}

